<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 自定义仿IOS的AlertDialog的样式 -->
    <style name="AlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>

        <item name="android:windowEnterAnimation">@anim/dialog_show_scale</item>
        <item name="android:windowExitAnimation">@anim/dialog_hide_scale</item>
        <item name="android:windowShowAnimation">@anim/dialog_show_scale</item>
        <!--        <item name="android:windowAnimationStyle">@style/windowAnim</item>-->
    </style>
</resources>